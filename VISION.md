# Product vision (template)

This repository is a **starter template**, not a shipped product. Use this file to capture what your fork is for: audience, problems you solve, and non-goals.

- **Stack**: pnpm workspaces, Turbo, Vite+, TanStack Start (Router + Query), React 19, Tailwind CSS.
- **Goal**: Clone, rename `AppConfig` in `apps/site/src/utils/system.tsx`, connect your API and auth, and ship.

Replace this document when you have a real product vision.
